# Backend API base URL — where this static site reads its data from.
# Leave EMPTY for local dev (Vite proxies /api and /uploads to localhost:3001).
# In production set it to the backend origin, e.g. https://api.greenoccasion.in
VITE_API_BASE_URL=
